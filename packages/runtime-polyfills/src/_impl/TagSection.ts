import { TagSection, Tag } from '@paperback/types'

globalThis.createTagSection = function (tagSection: TagSection): TagSection {
    return tagSection
}

globalThis.createTag = function (tag: Tag): Tag {
    return tag
}