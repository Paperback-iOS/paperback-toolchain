import { PartialSourceManga, IconText } from "@paperback/types"

// @ts-ignore
globalThis.createMangaTile = function (mangaTile: PartialSourceManga): PartialSourceManga {
    return mangaTile
}

globalThis.createPartialSourceManga = function (mangaTile: PartialSourceManga): PartialSourceManga {
    return mangaTile
}

globalThis.createIconText = function (iconText: IconText): IconText {
    return iconText
}